ID: PRRT_kwDOQMalTM5hM_2U
PATH: infra/aws-core/terraform/modules/eks-cluster/variables.tf
LINE: 183
RESOLVED: False OUTDATED: False
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580510
    The node_disk_size default value was changed from 50GB to 30GB without explanation. This change could impact workloads that require more disk space. Consider adding a comment explaining the rationale for this reduction (e.g., cost optimization, typical usage patterns) or documenting this breaking change in commit messages or release notes.
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_2Y
PATH: infra/aws-core/terraform/environments/dev/nat-polling-functions.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580514
    [nitpick] The exponential backoff calculation uses awk which may not be available in all environments. Consider using pure bash arithmetic: interval=$(( interval * 3 / 2 )) with a subsequent check: [[ $interval -gt $max_interval ]] && interval=$max_interval. This improves portability and readability.
    ```suggestion
            interval=$(( interval * 3 / 2 ))
            [[ $interval -gt $max_interval ]] && interval=$max_interval
    ```
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_2b
PATH: infra/aws-core/terraform/environments/dev/emergency-cleanup-improved.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580517
    [nitpick] The confirmation prompt uses 'DELETE EVERYTHING' which is inconsistent with the variable name. The original code checked for exact match with 'DELETE EVERYTHING', but it's better practice to use a more explicit confirmation string like 'yes-delete-all-resources' that's harder to type accidentally.
    ```suggestion
            read -r -p "Type 'yes-delete-all-resources' to confirm: " confirm

            if [[ "$confirm" != "yes-delete-all-resources" ]]; then
    ```
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_2e
PATH: .github/workflows/nightly-resource-audit.yml
LINE: 138
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580520
    [nitpick] The ARN parsing logic using 'rev' and multiple 'cut' commands is fragile and hard to understand. Consider using bash parameter expansion or a more explicit parsing approach: RESOURCE_ID=\"${arn##*/}\" for the last segment. This is more readable and less prone to edge cases.
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_5t
PATH: .github/workflows/nightly-resource-audit.yml
LINE: 138
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580754
    _🧹 Nitpick_ | _🔵 Trivial_

    **Harden ARN parsing logic against malformed ARNs.**

    Lines 127-134 parse resource type and ID from ARNs:
    ```bash
    RESOURCE_TYPE=$(echo "$arn" | cut -d: -f6 | cut -d/ -f1)
    RESOURCE_ID=$(echo "$arn" | rev | cut -d/ -f1 | rev)
    ```

    This is fragile because ARN format varies by resource type:
    - Standard: `arn:aws:ec2:region:account:nat-gateway/ngw-xxx` (works)
    - Alternative: `arn:aws:ec2:region:account:elastic-ip-allocation/eipalloc-xxx` (may break)
    - EKS: `arn:aws:eks:region:account:cluster/cluster-name` (works)

    Consider using `jq` (already available in workflow) to parse ARNs more robustly:

    ```bash
    RESOURCE_TYPE=$(echo "$arn" | jq -R 'split(":")[5] | split("/")[0]')
    RESOURCE_ID=$(echo "$arn" | jq -R 'split("/")[-1]')
    ```

    Or add explicit handling for known formats to catch parsing errors.

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    .github/workflows/nightly-resource-audit.yml around lines 127 to 134: the
    current ARN parsing using cut is brittle and can mis-extract resource types/IDs
    for varying ARN formats; replace the cut-based parsing with robust jq-based
    parsing or explicit format checks: use jq -R with split(":")[5] | split("/")[0]
    to get RESOURCE_TYPE and split("/")[-1] to get RESOURCE_ID (or implement
    explicit pattern matching for known services) and add a fallback/error branch
    when parsing yields empty or unexpected values so malformed ARNs are skipped or
    logged.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_5w
PATH: .github/workflows/nightly-resource-audit.yml
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580757
    _🧹 Nitpick_ | _🔵 Trivial_

    **Verify cost estimates are current and documented.**

    Lines 174-189 estimate monthly costs:
    ```
    NAT Gateway: ~$32/month
    Elastic IP (unused): ~$3.20/month
    EC2 t3.medium: ~$30/month
    ```

    These estimates should:
    1. **Be documented with source/date**: AWS pricing changes. Add comments with pricing source/date.
    2. **Account for region variations**: us-west-2 pricing may differ from other regions.
    3. **Be more conservative**: These appear to be rough estimates. Consider adding "(rough estimate)" to output.

    Add inline comments or documentation linking to AWS pricing pages, and consider making the script region-aware for cost calculations.

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    .github/workflows/nightly-resource-audit.yml around lines 174-189: the cost
    estimates are hardcoded without source/date, not region-aware, and not marked as
    rough estimates; update the file by adding inline comments above the estimates
    that include the AWS pricing source URLs and the date checked, make the script
    use a REGION variable (or derive AWS_DEFAULT_REGION) and choose per-region unit
    prices (fallback to the current values if region mapping missing), and change
    the echoed/output string to explicitly label the numbers as "(rough estimate)"
    so consumers know they are approximate.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_5y
PATH: .github/workflows/terraform-destroy-validation.yml
LINE: 161
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580759
    _⚠️ Potential issue_ | _🔴 Critical_

    **Fix undefined variable references in verbose logging output.**

    Variables `NAT_GATEWAYS`, `VPC_ENDPOINTS`, `EIPS`, and `SECURITY_GROUPS` are never defined. The corresponding count variables should be used instead (e.g., `$NAT_COUNT`).

    Apply this diff to fix the undefined variable references:

    ```diff
      if [ "${{ github.event.inputs.verbose_logging }}" == "true" ]; then
        echo "Pre-Destroy Resources:"
        echo "  EKS Cluster: $EKS_CLUSTER"
    -   echo "  NAT Gateways: $NAT_GATEWAYS"
    -   echo "  VPC Endpoints: $VPC_ENDPOINTS"
    -   echo "  Elastic IPs: $EIPS"
    -   echo "  Security Groups: $SECURITY_GROUPS"
    +   echo "  NAT Gateways: $NAT_COUNT"
    +   echo "  VPC Endpoints: $VPCE_COUNT"
    +   echo "  Elastic IPs: $EIP_COUNT"
    +   echo "  Security Groups: $SG_COUNT"
      fi
    ```

    <!-- suggestion_start -->

    <details>
    <summary>📝 Committable suggestion</summary>

    > ‼️ **IMPORTANT**
    > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

    ```suggestion
              if [ "${{ github.event.inputs.verbose_logging }}" == "true" ]; then
                echo "Pre-Destroy Resources:"
                echo "  EKS Cluster: $EKS_CLUSTER"
                echo "  NAT Gateways: $NAT_COUNT"
                echo "  VPC Endpoints: $VPCE_COUNT"
                echo "  Elastic IPs: $EIP_COUNT"
                echo "  Security Groups: $SG_COUNT"
              fi
    ```

    </details>

    <!-- suggestion_end -->

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    In .github/workflows/terraform-destroy-validation.yml around lines 150 to 157,
    the verbose logging prints undefined variables (NAT_GATEWAYS, VPC_ENDPOINTS,
    EIPS, SECURITY_GROUPS); replace these with the corresponding count variables so
    the output uses NAT_COUNT, VPC_ENDPOINT_COUNT, EIP_COUNT, and
    SECURITY_GROUP_COUNT (e.g., echo "  NAT Gateways: $NAT_COUNT") to avoid
    referencing undefined variables.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_53
PATH: .github/workflows/terraform-destroy-validation.yml
LINE: 198
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580764
    _⚠️ Potential issue_ | _🟡 Minor_

    **Exit code from destroy command is not properly captured.**

    Line 186 includes `|| { echo ...; exit 1; }`, which exits the step if destroy fails. Line 188 then attempts to capture `$?`, but this will always be `0` because the previous command either succeeded or already exited the step. The `destroy_exit_code` output is therefore not meaningful.

    Consider removing the redundant output or restructuring to capture the destroy outcome differently (e.g., by checking the actual Terraform state in Phase 6).

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    .github/workflows/terraform-destroy-validation.yml around lines 177 to 189: the
    step exits on failure via the `|| { ...; exit 1; }` so the subsequent `echo
    "destroy_exit_code=$?" >> "$GITHUB_OUTPUT"` always records 0 or never runs; fix
    by capturing terraform's exit code immediately after the command into a variable
    (e.g., rc=$?), then conditionally handle failure (echo message and exit if
    needed) and write that captured rc to GITHUB_OUTPUT, or simply remove the
    redundant output line if you don't need the exit code.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_55
PATH: infra/aws-core/terraform/environments/dev/emergency-cleanup-improved.sh
LINE: 42
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580766
    _🧹 Nitpick_ | _🔵 Trivial_

    **Enhance deprecation message clarity.**

    While the deprecation messaging is clear, the early exit (line 34-36) will silently fail with exit code 1 when `FORCE_DEPRECATED != "true"`. Users may not understand why the script failed.

    Add a more explicit message before exit:

    ```bash
    if [[ "${FORCE_DEPRECATED:-false}" != "true" ]]; then
        echo ""
        echo "❌ Script execution blocked (deprecated)"
        echo "Set FORCE_DEPRECATED=true to override (NOT RECOMMENDED)"
        exit 1
    fi
    ```

    This makes the failure reason explicit rather than implicit.

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    infra/aws-core/terraform/environments/dev/emergency-cleanup-improved.sh around
    lines 22 to 39: the script currently exits silently with code 1 when
    FORCE_DEPRECATED is not set, which may confuse users; before the exit add
    explicit echo lines that state the script is blocked because it is deprecated
    and instruct how to override (e.g., "❌ Script execution blocked (deprecated)"
    and "Set FORCE_DEPRECATED=true to override (NOT RECOMMENDED)"), then exit 1 so
    users understand why it failed.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_57
PATH: infra/aws-core/terraform/environments/dev/emergency-cleanup-improved.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580768
    _⚠️ Potential issue_ | _🟠 Major_

    **Verify AWS CLI queries filter by both tag AND cluster scope.**

    Lines 72 and 175 filter Elastic IPs by `ManagedBy=Terraform` tag:
    ```bash
    --query "Addresses[?Tags[?Key==\`ManagedBy\` && Value==\`Terraform\`]]"
    ```

    However, this doesn't filter by `Cluster=$CLUSTER_NAME`. If multiple clusters exist in the region, this query could delete IPs from OTHER clusters. Consider adding cluster tag filtering:

    ```bash
    --query "Addresses[?Tags[?Key==\`Cluster\` && Value==\`$CLUSTER_NAME\`] && Tags[?Key==\`ManagedBy\` && Value==\`Terraform\`]]"
    ```

    This is especially critical given that the emergency cleanup documentation (EMERGENCY_CLEANUP_BUGS.md, line 16) specifically calls out "Apply strict tag filters (cluster name + managed-by) to every AWS CLI call".



    Also applies to: 175-175

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    infra/aws-core/terraform/environments/dev/emergency-cleanup-improved.sh around
    lines 72 and 175: the AWS CLI describe-addresses queries only filter Elastic IPs
    by ManagedBy=Terraform, risking deletion across clusters; update both queries to
    also require Tags[?Key==`Cluster` && Value==`$CLUSTER_NAME`] so the query
    matches both cluster name and managed-by tag, ensure proper shell variable
    expansion and escaping in the --query string, and test the command with
    echo/dry-run to confirm it returns only IPs for the intended cluster before
    running destructive commands.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_5-
PATH: infra/aws-core/terraform/environments/dev/nat-polling-functions.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580772
    _🛠️ Refactor suggestion_ | _🟠 Major_

    **Consolidate duplicated logging functions across the codebase.**

    Logging functions (`log_info`, `log_warn`, `log_error`, `log_success`) are defined identically or near-identically in at least four other files (`scripts/validate-terraform-state.sh`, `scripts/verify-aws-resources-deleted.sh`, `scripts/validate-resource-tags.sh`, and `infra/aws-core/terraform/environments/dev/emergency-cleanup-improved.sh`). However, formatting and icon choices differ slightly between implementations.

    Extract these functions to a shared utility module (e.g., `scripts/logging.sh`) and source it from all scripts to maintain a single source of truth and reduce maintenance burden.

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    In infra/aws-core/terraform/environments/dev/nat-polling-functions.sh around
    lines 28 to 43, the four logging functions are duplicated across multiple
    scripts; extract them into a single shared file (e.g., scripts/logging.sh) that
    defines color variables and the functions (log_info, log_success, log_warn,
    log_error) with the chosen standard formatting/icons, then replace the local
    definitions in this file and the other scripts
    (scripts/validate-terraform-state.sh, scripts/verify-aws-resources-deleted.sh,
    scripts/validate-resource-tags.sh,
    infra/aws-core/terraform/environments/dev/emergency-cleanup-improved.sh) with a
    single line that sources the shared module (source "$(dirname
    "${BASH_SOURCE[0]}")/../../scripts/logging.sh" or correct relative path); ensure
    the shared module is idempotent (guard against double-sourcing), documents how
    to override functions if needed, and that any dependent color variables are
    declared or defaulted in the shared file so scripts relying on them keep
    working.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6A
PATH: infra/aws-core/terraform/environments/dev/nat-polling-functions.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580774
    _🧹 Nitpick_ | _🔵 Trivial_

    **Simplify exponential backoff calculation using bash arithmetic.**

    The exponential backoff calculation uses `awk` for floating-point arithmetic, which works but is harder to read and maintain:

    ```bash
    interval=$(awk "BEGIN {print int((($interval * 1.5) < $max_interval ? ($interval * 1.5) : $max_interval))}")
    ```

    Bash supports arithmetic expansion natively. Consider rewriting for clarity.

    Apply this diff to simplify the calculation:

    ```diff
    -interval=$(awk "BEGIN {print int((($interval * 1.5) < $max_interval ? ($interval * 1.5) : $max_interval))}")
    +new_interval=$(( interval * 3 / 2 ))  # 1.5x multiplier using integer arithmetic
    +interval=$(( new_interval > max_interval ? max_interval : new_interval ))
    ```

    Note: This uses integer arithmetic (`* 3 / 2` instead of `* 1.5`) to avoid floating-point operations while preserving the exponential backoff behavior.


    Also applies to: 231-231

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    infra/aws-core/terraform/environments/dev/nat-polling-functions.sh lines 151 and
    231: replace the awk-based floating-point backoff with pure bash integer
    arithmetic by computing a scaled value using interval*3/2 and then assigning
    interval to either that scaled value or max_interval whichever is smaller (e.g.,
    compute scaled = interval*3/2; if scaled < max_interval then interval=scaled
    else interval=max_interval), using bash arithmetic expansion and integer
    comparison to avoid awk and floating-point operations.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6C
PATH: infra/aws-core/terraform/environments/dev/nat-polling-functions.sh
LINE: 389
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580776
    _🧹 Nitpick_ | _🔵 Trivial_

    **Verify cleanup_all_nat_gateways handles edge cases for concurrent state changes.**

    The function reads NAT Gateway IDs at line 347-351, then initiates deletions at lines 368-371. If NAT Gateways are created or deleted by external processes between discovery and polling, the polling loop (lines 377-384) may encounter resource-not-found errors silently (depending on AWS CLI behavior).

    While this is likely intentional for robustness, ensure that transient AWS API errors (network issues, throttling) are handled consistently with the polling functions above, which retry on query errors. Consider adding explicit error logging for unexpected failures during parallel cleanup.

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6F
PATH: infra/aws-core/terraform/modules/eks-cluster/main.tf
LINE: 52
RESOLVED: False OUTDATED: False
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580779
    _🧹 Nitpick_ | _🔵 Trivial_

    **Add clarifying comment on addon merge semantics.**

    The conditional merge expression correctly handles IRSA-based EBS CSI Driver integration, but the logic deserves documentation to explain behavior when users explicitly configure `aws-ebs-csi-driver` in `cluster_addons`.



    ```diff
       # Cluster addons (automatically managed)
    +  # Merge user-provided addons with IRSA-enabled aws-ebs-csi-driver addon.
    +  # When IRSA is enabled: merges any user-specified aws-ebs-csi-driver config with service_account_role_arn.
    +  # When IRSA is disabled: uses only user-provided cluster_addons (if not already present, no default CSI driver).
       addons = merge(
         var.cluster_addons,
         var.enable_irsa ? {
           aws-ebs-csi-driver = merge(
             lookup(var.cluster_addons, "aws-ebs-csi-driver", {}),
             {
               service_account_role_arn = aws_iam_role.ebs_csi_driver[0].arn
             }
           )
         } : {}
       )
    ```

    <!-- suggestion_start -->

    <details>
    <summary>📝 Committable suggestion</summary>

    > ‼️ **IMPORTANT**
    > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

    ```suggestion
      # Merge user-provided addons with IRSA-enabled aws-ebs-csi-driver addon.
      # When IRSA is enabled: merges any user-specified aws-ebs-csi-driver config with service_account_role_arn.
      # When IRSA is disabled: uses only user-provided cluster_addons (if not already present, no default CSI driver).
      addons = merge(
        var.cluster_addons,
        var.enable_irsa ? {
          aws-ebs-csi-driver = merge(
            lookup(var.cluster_addons, "aws-ebs-csi-driver", {}),
            {
              service_account_role_arn = aws_iam_role.ebs_csi_driver[0].arn
            }
          )
        } : {}
      )
    ```

    </details>

    <!-- suggestion_end -->

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    In infra/aws-core/terraform/modules/eks-cluster/main.tf around lines 40 to 50,
    add a clarifying Terraform comment above the addons = merge(...) block
    explaining the merge semantics: note that when var.enable_irsa is true we merge
    an object that sets service_account_role_arn to the created IAM role ARN into
    any existing aws-ebs-csi-driver entry from var.cluster_addons, so user-provided
    fields in cluster_addons are preserved but the service_account_role_arn will be
    added or overwritten with the IRSA role; also mention that when enable_irsa is
    false nothing is merged and the user's aws-ebs-csi-driver config remains
    untouched.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6G
PATH: infra/aws-core/terraform/modules/eks-cluster/variables.tf
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580780
    _⚠️ Potential issue_ | _🔴 Critical_

    **Rename `putin_khuylo` variable to something appropriate for production code.**

    The variable name is offensive and inappropriate for a production codebase, regardless of its technical purpose. This needs immediate remediation before merging.

    Suggested alternatives that preserve the intent:
    - `upstream_module_gate_override`
    - `enable_module_creation_override`
    - `skip_upstream_gate_check`



    ```diff
    -variable "putin_khuylo" {
    -  description = "Override to keep module creation enabled even when the upstream module gate defaults to false."
    +variable "upstream_module_gate_override" {
    +  description = "Override upstream module gating to keep creation enabled. Set to true when upstream module defaults to false."
       type        = bool
       default     = true
     }
    ```

    This change must be propagated to:
    - `infra/aws-core/terraform/modules/eks-cluster/main.tf` (line 12)
    - `infra/aws-core/terraform/environments/dev/main.tf` (line 50)
    - `infra/aws-core/terraform/modules/eks-cluster/README.md` (line 357 and inputs table)

    <!-- suggestion_start -->

    <details>
    <summary>📝 Committable suggestion</summary>

    > ‼️ **IMPORTANT**
    > Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

    ```suggestion
    variable "upstream_module_gate_override" {
      description = "Override upstream module gating to keep creation enabled. Set to true when upstream module defaults to false."
      type        = bool
      default     = true
    }
    ```

    </details>

    <!-- suggestion_end -->

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6H
PATH: scripts/validate-destroy.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580781
    _🧹 Nitpick_ | _🔵 Trivial_

    **Broaden data-storage resource detection regex.**

    Lines 193-206 detect data-storage resources for deletion warnings using this pattern:
    ```bash
    STORAGE_DELETES=$(jq '[.resource_changes[] | select(.change.actions[] == "delete" and (.type | test("rds|dynamodb|elasticsearch|elasticache|redshift")))]
    ```

    This pattern catches common data stores but misses:
    - `aws_db_instance` (legacy RDS naming)
    - `aws_documentdb_cluster`
    - `aws_neptune_cluster`
    - `aws_memorydb_cluster`
    - `aws_opensearch_domain`
    - `aws_kinesis_stream`

    Consider expanding the regex or creating a more comprehensive list. Alternatively, add a comment noting this is not exhaustive and users should review the full plan.

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    In scripts/validate-destroy.sh around lines 193 to 206, the current jq regex for
    detecting data-storage resource deletions is too narrow and misses resource
    types like aws_db_instance, aws_documentdb_cluster, aws_neptune_cluster,
    aws_memorydb_cluster, aws_opensearch_domain, and aws_kinesis_stream; update the
    select filter to include those types (expand the regex OR list to cover these
    names or switch to a whitelist array of storage type strings and test
    membership), and/or add a clear comment above this block stating the list is not
    exhaustive and users must manually review the full plan; ensure the jq selection
    logic still checks .change.actions[] == "delete" and exits with the same warning
    behavior when matches are found.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6J
PATH: scripts/validate-destroy.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580784
    _🧹 Nitpick_ | _🔵 Trivial_

    **Account for potential state count discrepancies in before/after calculation.**

    Lines 254-260 calculate before/after state:
    ```bash
    CURRENT_STATE=$(terraform state list 2>/dev/null | wc -l || echo "unknown")
    FUTURE_STATE=$((CURRENT_STATE - DELETES))
    ```

    This assumes every resource in the deletion list corresponds to exactly one resource in the state. However:
    1. Some resources (e.g., `aws_security_group_rule`) may be auto-created and not explicitly listed
    2. Dependent resources may be implicitly destroyed
    3. State line count can be misleading (modules, for_each iterations)

    Consider either:
    1. Using `terraform plan -json` to get the actual final resource count
    2. Adding a caveat to the output ("approximately")
    3. Extracting the final resource count from the plan itself

    For now, adding a clarifying comment is minimum:
    ```bash
    echo "  Before: ~$CURRENT_STATE resources (approximate)"
    ```

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    In scripts/validate-destroy.sh around lines 254 to 265, the CURRENT_STATE /
    FUTURE_STATE count logic is unreliable because terraform state list line counts
    can be misleading; update the output to explicitly mark the counts as
    approximate (e.g., change the echoed "Before/After" lines to include
    "(approximate)"), and adjust the warning message to reflect counting
    approximation. Optionally, replace or augment this heuristic by running
    terraform plan -json and parsing resource_changes (count total resources and
    deletes) to compute an accurate future count; if you keep the simple approach,
    at minimum annotate the printed counts as approximate so users know they may be
    inaccurate.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6M
PATH: scripts/validate-resource-tags.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580787
    _🧹 Nitpick_ | _🔵 Trivial_

    **Optimize VPC Endpoint tag validation to avoid N+1 queries.**

    Lines 138-150 iterate over VPC endpoints, then for each endpoint make a separate `describe-vpc-endpoints` call. This causes unnecessary API calls (one query per endpoint).

    The initial query at lines 127-130 already returns all VPC endpoints. Instead, extract and validate tags from the first query results rather than querying again:

    ```bash
    # BEFORE: N+1 queries
    for vpce_id in $VPC_ENDPOINTS; do
      VPCE_TAGS=$(aws ec2 describe-vpc-endpoints --vpc-endpoint-ids "$vpce_id" ...)
      # ... check tags
    done

    # AFTER: Single query with post-processing
    aws ec2 describe-vpc-endpoints --region "$REGION" --query \
      'VpcEndpoints[].{Id: VpcEndpointId, Tags: Tags}' --output json | \
      jq '.[] | select(.Tags[] | select(.Key=="Cluster" and .Value==env.CLUSTER))'
    ```

    This reduces API calls and improves script performance.

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    In scripts/validate-resource-tags.sh around lines 138 to 150, the loop issues an
    AWS API call per VPC endpoint (N+1 queries); instead use the initial
    describe-vpc-endpoints result (from lines ~127-130) which already contains all
    VpcEndpoints and their Tags: parse that JSON (store it in a variable like
    ALL_VPC_ENDPOINTS_JSON), iterate or jq-filter against that JSON to count
    endpoints with a Tag Key=="Cluster" and Value== "$CLUSTER_NAME" (handle absence
    of Tags gracefully), increment TAGGED_VPCES from the parsed data (or compute it
    directly with a jq length expression) and remove the per-endpoint
    describe-vpc-endpoints calls to eliminate the extra API requests.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6P
PATH: scripts/validate-terraform-state.sh
LINE: 76
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580791
    _🧹 Nitpick_ | _🔵 Trivial_

    **Simplify EKS cluster detection logic.**

    Lines 77-81 search for `"aws_eks_cluster"` in terraform state list output. This works, but the nested if-else is verbose for a simple search:

    ```bash
    # Current (lines 75-82)
    if [ "$RESOURCE_COUNT" -gt 0 ]; then
        if terraform state list | grep -q "aws_eks_cluster" 2>/dev/null; then
            log_info "EKS cluster found in state"
        else
            log_warn "EKS cluster not found in state (may already be destroyed)"
        fi
    fi
    ```

    This can be simplified and made more robust:

    ```bash
    # Simpler approach
    if terraform state list 2>/dev/null | grep -q "aws_eks_cluster"; then
        log_info "EKS cluster found in state"
    elif [ "$RESOURCE_COUNT" -gt 0 ]; then
        log_warn "EKS cluster not found (may already be destroyed)"
    fi
    ```

    This eliminates redundant nesting and checks for EKS cluster independently of resource count.

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    In scripts/validate-terraform-state.sh around lines 74 to 82, the nested if that
    checks for an EKS cluster is verbose and tied to RESOURCE_COUNT; replace it with
    a single conditional that runs terraform state list (redirecting stderr) piped
    to grep -q "aws_eks_cluster" and if that succeeds call log_info "EKS cluster
    found in state", else if [ "$RESOURCE_COUNT" -gt 0 ] call log_warn "EKS cluster
    not found (may already be destroyed)"; ensure stderr is redirected to /dev/null
    when running terraform state list so failures are suppressed.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6T
PATH: scripts/validate-terraform-state.sh
LINE: 114
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580794
    _🧹 Nitpick_ | _🔵 Trivial_

    **Clarify terraform refresh error handling and placement.**

    Lines 105-116 run `terraform refresh` AFTER all validation checks. If refresh fails, the script exits with code 1, masking the earlier validation results.

    Additionally, the comment says "Look for broken references or dangling imports" but `terraform refresh` is primarily for state sync, not reference validation.

    Consider:
    1. Moving refresh earlier in the validation flow (before state analysis)
    2. Making refresh failures non-fatal (warn instead of exit) unless in destroy mode
    3. Adding a separate `terraform validate` step if reference checking is the goal

    Current placement suggests this step is supplementary, but its errors are fatal—clarify the intent and adjust accordingly.

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hM_6W
PATH: scripts/verify-aws-resources-deleted.sh
LINE: 89
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507580798
    _🛠️ Refactor suggestion_ | _🟠 Major_

    **Standardize logging function style across scripts.**

    The logging functions here use emoji (✓, ⚠, ✗) in the echo statements:
    ```bash
    log_info() { echo -e "${GREEN}✓${NC} $1"; }
    ```

    However, `infra/aws-core/terraform/environments/dev/nat-polling-functions.sh` uses:
    ```bash
    log_info() { echo -e "${BLUE}[INFO]${NC} $1"; }
    ```

    And `infra/aws-core/terraform/environments/dev/emergency-cleanup-improved.sh` uses:
    ```bash
    log_info() { echo -e "${GREEN}ℹ${NC} $1"; }
    ```

    This inconsistency makes output formatting fragmented across scripts. Establish a single logging standard and apply it consistently across all validation/cleanup scripts.

    <details>
    <summary>🤖 Prompt for AI Agents</summary>

    ```
    In scripts/verify-aws-resources-deleted.sh around lines 69 to 96 the logging
    helpers emit emoji-style prefixes (✓, ⚠, ✗) which is inconsistent with other
    scripts; standardize the logging format across validation/cleanup scripts by
    choosing one style (e.g. prefixed tags like [INFO], [WARN], [ERROR] with
    consistent color variables) and update the log_info/log_warn/log_error function
    implementations here to use that chosen format; then sweep the other scripts
    referenced (infra/aws-core/terraform/environments/dev/nat-polling-functions.sh
    and infra/aws-core/terraform/environments/dev/emergency-cleanup-improved.sh) and
    align their logging functions to the same implementation so all scripts produce
    uniform output.
    ```

    </details>

    <!-- This is an auto-generated comment by CodeRabbit -->
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hNIXX
PATH: scripts/logging.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507619949
    The logging functions only handle the first argument ($1). If callers pass multiple arguments or strings with spaces, only the first part will be logged. Consider using \"$*\" instead of \"$1\" to capture all arguments, or document that only a single quoted string should be passed.
    ```suggestion
      echo -e "${LOG_COLOR_BLUE}[INFO]${LOG_COLOR_RESET} $*"
    }

    log_success() {
      echo -e "${LOG_COLOR_GREEN}[SUCCESS]${LOG_COLOR_RESET} $*"
    }

    log_warn() {
      echo -e "${LOG_COLOR_YELLOW}[WARN]${LOG_COLOR_RESET} $*"
    }

    log_error() {
      echo -e "${LOG_COLOR_RED}[ERROR]${LOG_COLOR_RESET} $*"
    ```
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hNIXY
PATH: scripts/validate-terraform-state.sh
LINE: 111
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507619950
    The removal of `exit 1` after detecting refresh errors changes this from a fatal check to a warning. This is a significant behavior change that could allow corrupted state to pass validation. Consider documenting why refresh errors are now non-fatal, or add a conditional flag to control this behavior.
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hNIXa
PATH: scripts/validate-destroy.sh
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507619952
    The regex check validates that FUTURE_STATE is numeric before comparison, but the error message mentions 'approximate counts' which suggests the counting logic may be unreliable. Consider investigating and fixing the root cause of negative counts rather than just warning about them.
    ```suggestion
      print_error "Expected future state is negative (counting error detected). Aborting validation."
      exit 1
    ```
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hNIXb
PATH: infra/aws-core/terraform/modules/eks-cluster/main.tf
LINE: 52
RESOLVED: False OUTDATED: False
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507619953
    [nitpick] The nested merge uses `aws_iam_role.ebs_csi_driver[0].arn` which assumes the resource exists (conditional count=1). If `enable_irsa` is false or the addon is not configured, this could cause an index error. The logic appears correct with the conditional, but consider adding a comment explaining the safety of this pattern for future maintainers.
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hNIXc
PATH: infra/aws-core/terraform/modules/eks-cluster/iam.tf
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507619954
    The regex pattern `/^(.*provider/)/` extracts the OIDC provider ID from the ARN, but this pattern is fragile and relies on ARN format stability. Consider using a more explicit extraction method or documenting the expected ARN format for future reference.
    ```suggestion
          variable = "${split("oidc-provider/", module.eks.oidc_provider_arn)[1]}:sub"
          values   = ["system:serviceaccount:kube-system:ebs-csi-controller-sa"]
        }

        condition {
          test     = "StringEquals"
          variable = "${split("oidc-provider/", module.eks.oidc_provider_arn)[1]}:aud"
    ```
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hNIXd
PATH: infra/aws-core/terraform/environments/dev/nat-polling-functions.sh
LINE: 141
RESOLVED: False OUTDATED: False
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507619955
    [nitpick] The replacement of `awk` with bash arithmetic (`interval * 3 / 2`) is an improvement, but the calculation produces integer division which may not accurately represent 1.5x multiplier for small intervals (e.g., 5 * 3 / 2 = 7, not 7.5). Consider adding a comment explaining that integer truncation is intentional for simplicity.
--------------------------------------------------------------------------------
ID: PRRT_kwDOQMalTM5hNIXe
PATH: .github/workflows/terraform-destroy-validation.yml
LINE: None
RESOLVED: False OUTDATED: True
  URL: https://github.com/wlevan3/ml-platform-engineering-practicum/pull/141#discussion_r2507619956
    [nitpick] The shellcheck disable comment `SC2126` is added for this line, but using `awk` to count lines is actually more robust than `wc -l` for handling edge cases (empty lines, no newline at EOF). Consider removing the disable comment and acknowledging this is the correct approach, or document why the specific shellcheck rule is being disabled.
    ```suggestion
              # shellcheck disable=SC2086
    ```
-----------------------------------------------------------------------------------
Resolution log (added by Codex):
1. PRRT_kwDOQMalTM5hM_2U — Added inline explanation for the node_disk_size default drop from 50GB to 30GB to highlight cost/usage rationale (infra/aws-core/terraform/modules/eks-cluster/variables.tf ~183).
2. PRRT_kwDOQMalTM5hM_6F — Documented the addons merge semantics, describing the IRSA-enabled aws-ebs-csi-driver merge behavior and guarded the service_account_role_arn lookup comment (infra/aws-core/terraform/modules/eks-cluster/main.tf ~52).
3. PRRT_kwDOQMalTM5hNIXb — Clarified that aws_iam_role.ebs_csi_driver[0] is safe because the role only exists when var.enable_irsa is true (same file/region).
4. PRRT_kwDOQMalTM5hNIXd — Emphasized that integer truncation in the nat-polling backoff is deliberate to keep the sleep interval whole-numbered.
