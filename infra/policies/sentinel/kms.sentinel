import "tfplan/v2" as tfplan

violations = func() {
    denies = []

    for tfplan.resource_changes as address, rc {
        if rc.type is "aws_kms_key" and rc.mode is "managed" {
            delete_planned = false
            if rc.change.actions {
                for rc.change.actions as action {
                    if action is "delete" {
                        delete_planned = true
                    }
                }
            }

            if not delete_planned {
                append(denies, sprintf("aws_kms_key %q is not allowed. Use AWS-managed encryption instead.", [address]))
            }
        }
    }

    return denies
}()

main = rule {
    length(violations) is 0
}
